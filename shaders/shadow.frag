#version 450

// ============================================================
// Shadow Map 片段着色器（最小化版本）
// ============================================================
// 作用：虽然 Shadow Pass 不需要输出颜色，但 Vulkan 的图形管线
//       仍然要求绑定片段着色器。这里输出一个空的 vec4。
//       实际深度由 GPU 自动写入深度附件。
// ============================================================

// 注意：阴影 Pass 没有颜色附件，因此不能有任何 layout(location=...) out 声明
// 深度由 GPU 硬件自动写入深度附件，此着色器仅为了满足管线创建需求
void main() {
    // 空实现 —— 深度由硬件自动写入
}
